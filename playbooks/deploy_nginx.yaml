---
- name: Deploy monitoring Nginx gateway
  hosts: "{{ target_hosts | default('all') }}"
  become: true
  gather_facts: true
  vars_files:
    - "../vars/common_{{ env | default('dev') }}.yaml"
  vars:
    nginx_gateway_enabled: "{{ (monitoring_components_enabled | default({})).nginx | default(true) | bool }}"

  tasks:
    - name: Validate Nginx host operating system
      ansible.builtin.assert:
        that:
          - ansible_os_family == 'Debian' or (ansible_os_family == 'RedHat' and (ansible_distribution_major_version | int) >= 8)
        fail_msg: >-
          Unsupported operating system '{{ ansible_distribution }}
          {{ ansible_distribution_version }}' on {{ inventory_hostname }}.
          Supported platforms are Debian-family systems and RHEL-family version
          8 or newer.

    - name: Install Nginx
      ansible.builtin.package:
        name: nginx
        state: present
      when: nginx_gateway_enabled | bool

    - name: Install self-signed certificate tooling
      ansible.builtin.package:
        name: openssl
        state: present
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool
        - nginx_generate_self_signed_cert | bool

    - name: Install RHEL SELinux management tooling
      ansible.builtin.package:
        name: policycoreutils-python-utils
        state: present
      when:
        - nginx_gateway_enabled | bool
        - ansible_os_family == 'RedHat'
        - (ansible_selinux | default({})).status | default('disabled') == 'enabled'
        - nginx_selinux_allow_network_connect | default(true) | bool

    - name: Read Nginx SELinux network-connect policy
      ansible.builtin.command:
        argv:
          - getsebool
          - httpd_can_network_connect
      register: nginx_selinux_network_connect
      changed_when: false
      when:
        - nginx_gateway_enabled | bool
        - ansible_os_family == 'RedHat'
        - (ansible_selinux | default({})).status | default('disabled') == 'enabled'
        - nginx_selinux_allow_network_connect | default(true) | bool

    - name: Allow Nginx to connect to monitoring upstreams under SELinux
      ansible.builtin.command:
        argv:
          - setsebool
          - -P
          - httpd_can_network_connect
          - "on"
      when:
        - nginx_gateway_enabled | bool
        - ansible_os_family == 'RedHat'
        - (ansible_selinux | default({})).status | default('disabled') == 'enabled'
        - nginx_selinux_allow_network_connect | default(true) | bool
        - "'--> on' not in nginx_selinux_network_connect.stdout"

    - name: Create Nginx TLS directory
      ansible.builtin.file:
        path: "{{ nginx_tls_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool

    - name: Generate self-signed gateway certificate
      ansible.builtin.command:
        argv:
          - openssl
          - req
          - -x509
          - -nodes
          - -newkey
          - rsa:2048
          - -keyout
          - "{{ nginx_ssl_certificate_key }}"
          - -out
          - "{{ nginx_ssl_certificate }}"
          - -days
          - "{{ nginx_self_signed_cert_days | string }}"
          - -subj
          - "/CN={{ monitoring_gateway_server_name }}"
          - -addext
          - "subjectAltName=DNS:{{ monitoring_gateway_server_name }}"
      args:
        creates: "{{ nginx_ssl_certificate }}"
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool
        - nginx_generate_self_signed_cert | bool
      notify: Reload Nginx

    - name: Set gateway certificate permissions
      ansible.builtin.file:
        path: "{{ item.path }}"
        owner: root
        group: root
        mode: "{{ item.mode }}"
      loop:
        - path: "{{ nginx_ssl_certificate }}"
          mode: "0644"
        - path: "{{ nginx_ssl_certificate_key }}"
          mode: "0600"
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool
        - nginx_generate_self_signed_cert | bool

    - name: Check gateway TLS files
      ansible.builtin.stat:
        path: "{{ item }}"
      loop:
        - "{{ nginx_ssl_certificate }}"
        - "{{ nginx_ssl_certificate_key }}"
      register: nginx_gateway_tls_files
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool

    - name: Require gateway TLS files
      ansible.builtin.assert:
        that:
          - nginx_gateway_tls_files.results | map(attribute='stat.exists') | min
        fail_msg: >-
          Nginx TLS is enabled, but {{ nginx_ssl_certificate }} and/or
          {{ nginx_ssl_certificate_key }} is missing. Provide these files or set
          nginx_generate_self_signed_cert: true for non-production environments.
      when:
        - nginx_gateway_enabled | bool
        - monitoring_gateway_tls_enabled | bool

    - name: Install monitoring gateway config
      ansible.builtin.template:
        src: "{{ playbook_dir }}/../configs/monitoring-gateway.conf.j2"
        dest: "{{ nginx_config_dir }}/monitoring-gateway.conf"
        owner: root
        group: root
        mode: "0644"
      when: nginx_gateway_enabled | bool
      notify: Reload Nginx

    - name: Validate Nginx config
      ansible.builtin.command: nginx -t
      changed_when: false
      when: nginx_gateway_enabled | bool

    - name: Enable and start Nginx
      ansible.builtin.systemd:
        name: nginx
        enabled: true
        state: started
        daemon_reload: true
      when: nginx_gateway_enabled | bool

  handlers:
    - name: Reload Nginx
      ansible.builtin.systemd:
        name: nginx
        state: reloaded
      when: nginx_gateway_enabled | bool
